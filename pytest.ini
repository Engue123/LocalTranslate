[pytest]
# Add the repository root to sys.path so `import core`, `plugins`, etc. resolve
# without needing PYTHONPATH set on the command line (works in CI and locally).
pythonpath = .
testpaths = tests
addopts = -v
